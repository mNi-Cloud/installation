apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Custom resources that must be applied after their owning CRDs/controllers.
resources:
  - ../vpc/bootstrap
  - ../vm/bootstrap
  - ../lb/bootstrap
  - ../k8s/bootstrap
